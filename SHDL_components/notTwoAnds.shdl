use twoAnds::{TwoAnds};
use stdgates::{NOT};

component NotTwoAnds(A, B, C) -> (Out){
    ta: TwoAnds;
    not1: NOT;

    connect{
        A -> ta.A;
        B -> ta.B;
        C -> ta.C;
        ta.Out->not1.A;
        not1.O->Out;
    }
}