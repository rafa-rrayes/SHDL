[project]
name = "shdlc"
version = "0.1.0"
description = "SHDL to Base SHDL flattener: the front-end of the SHDL toolchain"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
shdl-flatten = "flattener.cli:main"
shdlc = "shdlc.cli:main"
shdl-conformance = "conformance.runner.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["flattener", "shdlc", "conformance"]

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
